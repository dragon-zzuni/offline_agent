# 다이어그램을 PNG로 변환하는 방법

이 폴더에는 ARCHITECTURE_PRESENTATION.md의 Mermaid 다이어그램들이 개별 파일로 저장되어 있습니다.

## 📁 파일 목록

1. **system_architecture.mmd** - 전체 시스템 구조
2. **layer_architecture.mmd** - 레이어 구조
3. **detailed_structure.mmd** - offline_agent 상세 구조
4. **data_flow.mmd** - 데이터 플로우 (시퀀스 다이어그램)

---

## 🎨 방법 1: 온라인 변환 (가장 쉬움, 추천!)

### Mermaid Live Editor 사용

1. **웹사이트 접속**: https://mermaid.live/
2. **.mmd 파일 열기**: 메모장으로 파일 내용 복사
3. **왼쪽 에디터에 붙여넣기**: 자동으로 다이어그램 렌더링
4. **PNG 다운로드**: 
   - 우측 상단 "Actions" 버튼 클릭
   - "PNG" 선택
   - 해상도 선택 (2x 또는 4x 권장)
   - 다운로드

**장점:**
- 설치 불필요
- 즉시 사용 가능
- 고해상도 지원 (4x = 4배 확대)
- 배경색 선택 가능 (흰색/투명)

---

## 🖥️ 방법 2: Mermaid CLI (로컬 변환)

### 설치

```bash
# Node.js 설치 필요 (https://nodejs.org/)
npm install -g @mermaid-js/mermaid-cli
```

### 사용법

```bash
# 개별 파일 변환
mmdc -i system_architecture.mmd -o system_architecture.png -w 1920 -H 1080 -b white

# 또는 배치 파일 실행 (Windows)
generate_diagrams.bat
```

**옵션:**
- `-i`: 입력 파일 (.mmd)
- `-o`: 출력 파일 (.png)
- `-w`: 너비 (픽셀)
- `-H`: 높이 (픽셀)
- `-b`: 배경색 (white, transparent 등)

**권장 해상도:**
- 전체 구조: 1920x1080
- 레이어 구조: 1920x1080
- 상세 구조: 2400x1600 (복잡함)
- 데이터 플로우: 2400x1600 (시퀀스)

---

## 🌐 방법 3: VS Code 확장 (개발자용)

### 설치

1. VS Code에서 "Mermaid Preview" 확장 설치
2. .mmd 파일 열기
3. `Ctrl+Shift+P` → "Mermaid: Preview"
4. 우클릭 → "Save as PNG"

---

## 📊 방법 4: 온라인 도구 (대안)

### Kroki (https://kroki.io/)

```bash
# curl로 PNG 생성
curl -X POST https://kroki.io/mermaid/png -d @system_architecture.mmd -o system_architecture.png
```

### Mermaid Chart (https://www.mermaidchart.com/)

- 회원가입 필요
- 고급 편집 기능
- 팀 협업 지원

---

## 🎯 PPT/PDF에 삽입하기

### PowerPoint

1. PNG 파일 생성 (위 방법 중 하나)
2. PPT에서 "삽입" → "그림"
3. PNG 파일 선택
4. 크기 조정 (Shift 누르고 드래그 = 비율 유지)

**권장 설정:**
- 해상도: 2x 또는 4x (고해상도)
- 배경: 흰색 (PPT 배경과 일치)
- 크기: 슬라이드 너비의 80-90%

### PDF

1. PNG 파일 생성
2. Word/PowerPoint에 삽입
3. "파일" → "다른 이름으로 저장" → "PDF"

---

## 🎨 스타일 커스터마이징

### 색상 변경

Mermaid 파일에서 `style` 라인 수정:

```mermaid
style GUI fill:#4A90E2,color:#fff
style LLM fill:#E94B3C,color:#fff
```

### 폰트 크기 조정

```mermaid
%%{init: {'theme':'base', 'themeVariables': { 'fontSize':'16px'}}}%%
```

---

## 📝 빠른 시작 (추천 워크플로우)

### 발표 자료용 (5분 안에 완료)

1. **Mermaid Live Editor** 접속 (https://mermaid.live/)
2. 각 .mmd 파일 내용 복사 → 붙여넣기
3. "Actions" → "PNG" → "4x" 선택
4. 다운로드 (파일명: system_architecture_4x.png)
5. PPT에 삽입

### 고품질 출력용 (10분)

1. **Mermaid CLI** 설치
2. `generate_diagrams.bat` 실행
3. 생성된 PNG 파일 확인
4. 필요시 해상도 조정 후 재생성

---

## 🔧 트러블슈팅

### 문제: 한글이 깨짐
**해결:** 
- Mermaid Live Editor 사용 (한글 지원 우수)
- 또는 폰트 설정 추가:
  ```mermaid
  %%{init: {'theme':'base', 'themeVariables': { 'fontFamily':'Malgun Gothic'}}}%%
  ```

### 문제: 다이어그램이 잘림
**해결:**
- 해상도 증가 (`-w 3000 -H 2000`)
- 또는 온라인 에디터에서 4x 해상도 사용

### 문제: CLI 설치 실패
**해결:**
- Node.js 최신 버전 설치 확인
- 관리자 권한으로 실행
- 또는 온라인 도구 사용 (설치 불필요)

---

## 📚 참고 자료

- Mermaid 공식 문서: https://mermaid.js.org/
- Mermaid Live Editor: https://mermaid.live/
- Mermaid CLI: https://github.com/mermaid-js/mermaid-cli
- 예제 갤러리: https://mermaid.js.org/ecosystem/integrations.html

---

**작성일**: 2025-10-31  
**버전**: v1.0
